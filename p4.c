int main()
{
    //return 0;
    //return (0);
    return(0);
    //return0; this will give an undeclared error as we have changed the keyword
     // this means the same as : return 0; 
    // mind the space is there when returning without using bracketsS
    // when using with brackets it works the same way when we use it with or without space
}